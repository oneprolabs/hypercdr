# Third Party

Third-party dependency notes and integration metadata.

- `velero/`: Velero dependency notes. The full Velero source is maintained in
  the sibling `../hypercdr-velero/` repository. This platform repository keeps
  only the CRDs and image references required at runtime.
