services:
  bootstrap-portal:
    image: nginx:1.27-alpine
    container_name: hypercdr-bootstrap-portal
    restart: unless-stopped
    ports:
      - "${HCDR_BOOTSTRAP_PORT:-8080}:80"
    volumes:
      - ${HCDR_BOOTSTRAP_PORTAL_DIR:-/opt/hypercdr-bootstrap/portal}:/usr/share/nginx/html:ro
