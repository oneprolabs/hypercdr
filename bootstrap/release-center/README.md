# Release Center

This independent file-backed service accepts immutable release manifests and
provides catalog and manifest endpoints. Set `RELEASE_CENTER_TOKEN` to require
Bearer authentication and mount `RELEASE_CENTER_DATA` as persistent storage.
