import type { ReactNode } from 'react';
import { X } from 'lucide-react';
import { motion } from 'motion/react';

export function ModalFrame({ title, subtitle, icon, children, onClose, maxWidthClass = 'max-w-2xl' }: { title:string; subtitle?:string; icon?:ReactNode; children:ReactNode; onClose:()=>void; maxWidthClass?:string }) {
  return <div className="fixed inset-0 z-50 flex items-center justify-center p-6"><motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="absolute inset-0 bg-slate-950/25" onClick={onClose}/><motion.div initial={{opacity:0,y:16,scale:.97}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:16,scale:.97}} transition={{duration:.18,ease:'easeOut'}} className={`relative w-full ${maxWidthClass} overflow-hidden rounded-2xl bg-white shadow-2xl ring-1 ring-slate-900/5`}><div className="relative overflow-hidden border-b border-slate-100 bg-gradient-to-br from-slate-50 via-white to-slate-50 px-6 py-5"><div className="relative flex items-start justify-between gap-4"><div className="flex items-start gap-3">{icon&&<div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-gradient-to-br from-blue-500 to-blue-600 text-white shadow-lg shadow-blue-200">{icon}</div>}<div><h3 className="text-lg font-black leading-tight text-slate-900">{title}</h3>{subtitle&&<p className="mt-1 text-sm text-slate-500">{subtitle}</p>}</div></div><button onClick={onClose} className="rounded-lg p-2 text-slate-400 transition-colors hover:bg-white hover:text-slate-700"><X size={18}/></button></div></div><div className="p-6">{children}</div></motion.div></div>;
}
