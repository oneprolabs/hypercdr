# WebSocket Contracts

The canonical Go message types are in `backend/internal/protocol` and
`agent/comm-agent/pkg/protocol`. See
`docs/protocols/platform-agent-messages.md` for the reviewed contract.
