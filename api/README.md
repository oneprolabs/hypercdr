# API Contracts

Machine-readable REST and WebSocket contracts belong in this directory.
Human-readable protocol and state-machine documentation is maintained under
`docs/protocols`.

- REST/OpenAPI: `api/openapi`
- WebSocket schemas: `api/websocket`
