# HyperCDR Managed-Cluster Chart

This directory is reserved for the managed-cluster Helm chart. The platform's
generated registration installer remains the canonical installation path until
the chart templates and compatibility tests are completed.
